我们的全栈项目骨架已经搭建完毕。现在，请你帮我把后端 AiChatService 中的模拟回复逻辑，替换为调用真实大模型（DeepSeek）的 API 代码。

请严格按照以下步骤读取并修改 backend 文件夹下的代码：

第一步：配置密钥
在 src/main/resources/application.yml 中添加自定义配置：
ai:
api-key: sk-e0f53573bbff4ebb82b3bfc80decc77a
api-url: https://api.deepseek.com/chat/completions

第二步：添加网络请求工具
在 com.study.ai.config 包下新建一个 RestTemplateConfig 类，配置并注入一个 RestTemplate 的 Bean，用于发送 HTTP 请求。

第三步：重写核心逻辑
修改 AiChatService.java：

读取 application.yml 中的 api-key 和 api-url。

在 chat(String message) 方法中，利用 RestTemplate 向 api-url 发送 POST 请求。

请求头的 Authorization 需要设置为 Bearer 加上 api-key，Content-Type 设为 application/json。

请求体的 JSON 结构请遵循标准的 OpenAI 格式：包含 "model": "deepseek-chat"，以及 "messages" 数组（将用户的 message 放入 role 为 user 的 content 中）。

使用我们项目里已有的 fastjson2 库，解析大模型返回的 JSON 数据，提取出 choices[0].message.content 中的文本内容。

添加 try-catch 异常捕获逻辑，如果请求失败，请在控制台打印错误日志，并给前端返回“AI 大脑暂时走神了，请稍后再试”的提示语。

请在遇到导包问题或依赖冲突时自行解决。修改完成后，请告诉我重启后端服务即可。